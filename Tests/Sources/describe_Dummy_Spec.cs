using NSpec;

namespace Tests.Sources {
public class describe_WinSystem_Spec : nspec {
  void when_Startup() {
    before = () => {

    };
    it["always passes"] = () => {
      // Given
      // When
      // Then
      true.should_be(true);
    };
  }
}
}
